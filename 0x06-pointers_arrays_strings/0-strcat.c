#include "main.h"

/**
 * _strcat - Function main entry
 *
 * @dest: Variable String
 * @srcL Variable String
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
	char *str;

	dest[] = "";
	src[] = "";
	str = strcat(dest,src);
	return (0);
}
