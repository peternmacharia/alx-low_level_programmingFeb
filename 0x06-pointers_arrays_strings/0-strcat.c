#include "main.h"

/**
 * _strcat - Function main entry
 *
 * @dest: Variable String
 * @src: Variable String
 *
 * Return: 0
 */
char *_strcat(char *dest, char *src)
{
	int i, j;

	i = -1;
	while(dest[++1]);

	j = 0;
	while(dest[i++] = src[j++]);
	printf("%s", dest);
	return (0);
}
